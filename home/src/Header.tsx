import React from "react";

export const Header= ()=>{
    return(
        <div className="p-5 bg-blue-500 text-white text-3xl font-bold">
            Hello from Header, header is changed now
        </div>
    );
}
